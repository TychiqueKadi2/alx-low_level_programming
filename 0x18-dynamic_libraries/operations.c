#include <stdio.h>

/**
 * add - adds two intergers
 * @x: first int
 * @y: seconf int
 * Return: sum
 */
int add(int x, int y)
{
	return (x + y);
}

/**
 * sub - subyracts two intergers
 * @x: first int
 * @y: seconf int
 * Return: difference
 */
int sub(int x, int y)
{
        return (x - y);
}

/**
 * mod - mod of two intergers
 * @x: first int
 * @y: seconf int
 * Return: mod
 */
int mod(int x, int y)
{
        return (x % y);
}

/**
 * mul - mulutiplies two intergers
 * @x: first int
 * @y: seconf int
 * Return: multiplication
 */
int mul(int x, int y)
{
        return (x * y);
}

/**
 * div - divides two intergers
 * @x: first int
 * @y: seconf int
 * Return: division
 */
int div(int x, int y)
{
        return (x / y);
}
