#include "main.h"
/**
 * _abs - computes value of interger
 * @n: interger to be checked
 * Return: absulote value of interger
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
	return (-n);
	}
}
